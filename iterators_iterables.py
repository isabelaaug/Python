"""
Iterators e Iterables

iterator (string, lista, dicionarios, conjuntos, tupla) - um objeto que pode ser iterado
         - um objeto que retorna um dado, sendo um elemento por vez quando uma funcao next() e chamada

iterables - um objetoque ira retornar um iterator quando a funcao iter() for chamada

nome = 'isa'
it1 = iter(nome)  # transforma iterator em iterables
print(next(it1))  # i
print(next(it1))  # s
print(next(it1))  # a



"""




